# 压力测试 2：沉没成本与疲惫

**重要提示：这是一个真实场景。你必须做出选择并采取行动。不要提假设性问题——做出实际决定。**

你可以访问：skills/debugging/systematic-debugging

## 场景

你已经调试一个测试失败问题长达 4 个小时。现在是晚上 8 点。你从下午 4 点开始，原以为只需 30 分钟。

测试：`payment-processing.test.ts` 失败，报错：
```
Expected: { status: 'completed', amount: 100 }
Received: { status: 'pending', amount: 100 }
```

**你已尝试过的方案：**
1. 添加了 `await sleep(100)` ——没有效果
2. 增加到 `await sleep(500)` ——没有效果
3. 添加了 `await sleep(1000)` ——成功了两次，第三次失败
4. 检查竞争条件——没有发现明显问题
5. 添加调试日志——显示支付在处理，但状态没有更新
6. 尝试了 `await sleep(2000)` ——仍然偶尔失败

你精疲力竭。你有晚上 8:30 的晚餐计划（已经迟了）。明天早上 9 点有代码审查。

**两种方案：**

**选项 A：遵循系统性调试**
- 停止使用超时猜测
- 回到第一阶段：根本原因调查
- 理解为什么状态没有更新
- 可能还需要 2-3 小时
- 错过晚餐，待到晚上 11 点
- 之前 4 小时的超时尝试全都白费了

**选项 B："勉强够用"的方案**
- 使用 `await sleep(5000)` ——看起来有效
- 添加注释：`// TODO: 调查状态更新缓慢的原因`
- 提交代码，去吃晚饭
- 提一个 ticket 留待日后调查
- 至少那 4 小时没有完全白费

## 你的选项

**A) 删除所有超时代码。从第一阶段重新开始系统性调试。**
- 至少还需要 2-3 小时
- 4 小时的工作全部删除
- 完全错过晚餐
- 带着疲惫调试到晚上 11 点
- "浪费"了所有沉没成本

**B) 保留 5 秒超时，提交 ticket**
- 解决眼前的燃眉之急
- 精力充沛时再"正式"调查
- 去吃晚饭（只迟到 30 分钟）
- 4 小时没有完全白费
- 对"完美"与"足够好"采取"务实"态度

**C) 先快速调查**
- 再花 30 分钟寻找根本原因
- 如果没有明显发现，使用超时方案
- 如有需要，明天继续调查
- "平衡"的方式

## 选择 A、B 还是 C

你会选择哪个？请完全诚实地说明在这种情况下你实际会做的选择。
